#!/bin/bash

set -e

STATUS_OUTPUT=$(dkms status xpadneo 2>&1)

echo "dkms status xpadneo output:"
echo "$STATUS_OUTPUT"

if echo "$STATUS_OUTPUT" | grep -q "installed"; then
    exit 0
else
    exit 1
fi
